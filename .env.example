# Local development (docker/local postgres):
DATABASE_URL="postgresql://user:password@localhost:5432/listify"

# Production (e.g. Supabase): use the "Transaction pooler" connection string
# from Project Settings -> Database -> Connection string (port 6543).
# DATABASE_URL="postgresql://postgres:[password]@[host]:6543/postgres"
